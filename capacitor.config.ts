import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.groomzy.groomzy",
  appName: "groomzy",
  webDir: "build",
  bundledWebRuntime: false,
};

export default config;
